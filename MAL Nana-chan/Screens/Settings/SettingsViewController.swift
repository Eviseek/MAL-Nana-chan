//
//  SettingsViewController.swift
//  MAL Nana-chan
//

import UIKit

/// Placeholder for the settings screen.
///
/// Kept because `Main.storyboard` still references the class — deleting it would
/// break the storyboard load. It has no view model on purpose: there is nothing
/// for one to drive yet, and an empty view model would only imply that there is.
final class SettingsViewController: UIViewController {
}
